# Appwrite Console → API keys: enable "Sessions (write)" for email OTP and OAuth from this app.
APPWRITE_API_KEY=
NEXT_PUBLIC_APPWRITE_ENDPOINT=
NEXT_PUBLIC_APPWRITE_PROJECT_ID=
# Auth → OAuth2 — add success URLs, e.g. http://localhost:3000/api/auth/callback/google and .../callback/apple
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
VERCEL_TOKEN=
